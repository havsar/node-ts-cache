{
    "name": "root",
    "private": true,
    "scripts": {
        "bootstrap": "lerna bootstrap --strict --no-ci",
        "clean": "lerna clean -y",
        "test": "lerna run test --no-ci",
        "ls": "lerna ls",
        "lint": "prettier --check .",
        "format": "prettier --write ."
    },
    "devDependencies": {
        "lerna": "^3.22.1",
        "prettier": "^2.1.2"
    },
    "dependencies": {}
}
