{
    "name": "node-ts-cache",
    "private": true,
    "scripts": {
        "test": "npm run test --workspaces --no-ci --stream --if-present",
        "lint": "prettier --check .",
        "format": "prettier --write ."
    },
    "devDependencies": {
        "prettier": "^2.4.1"
    },
    "dependencies": {},
    "workspaces": [
        "packages/node-ts-cache",
        "packages/node-ts-cache-storage-ioredis",
        "packages/test"
    ]
}
